# Claude Code Skill Registry - 系统设计文档

## 1. 系统概述

### 1.1 项目简介

Claude Code Skill Registry 是一个基于 FastAPI 构建的私有技能插件注册中心，用于企业内部管理和分发 Claude Code 的技能插件。系统支持批量上传、版本管理、下载统计等功能。

### 1.2 核心功能

- **插件管理**: 上传、下载、删除技能插件
- **版本管理**: 支持多版本插件共存
- **下载统计**: 完整的下载行为追踪和统计分析
- **Web界面**: 用户友好的管理界面
- **API兼容**: 与 Claude Code 市场 API 兼容
- **批量操作**: 支持批量上传和批量下载
- **数据导出**: Excel 格式统计报表导出

### 1.3 技术栈

| 组件 | 技术选型 |
|------|----------|
| 后端框架 | FastAPI (Python 3.x) |
| Web 服务器 | Uvicorn |
| 数据库 | SQLite |
| 模板引擎 | Jinja2 |
| 前端 | HTML + CSS + JavaScript (无框架依赖) |
| 容器化 | Docker + Docker Compose |
| 文件存储 | 本地文件系统 |

---

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        Client Layer                         │
├─────────────────────────────────────────────────────────────┤
│  Web Browser          │  Claude Code CLI                   │
│  - Admin UI           │  - marketplace.json                │
│  - Plugin Browser     │  - plugin download                 │
└───────────┬───────────────────────────┬─────────────────────┘
            │                           │
            ▼                           ▼
┌─────────────────────────────────────────────────────────────┐
│                     FastAPI Application                     │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────┐  ┌─────────────┐  ┌──────────────┐       │
│  │   Public    │  │   Admin     │  │   Statistics │       │
│  │     API     │  │     API     │  │      API     │       │
│  └─────────────┘  └─────────────┘  └──────────────┘       │
│                                                               │
│  ┌─────────────┐  ┌─────────────┐  ┌──────────────┐       │
│  │  Auth       │  │  Plugin     │  │  Download    │       │
│  │  Middleware │  │  Scanner    │  │  Tracker     │       │
│  └─────────────┘  └─────────────┘  └──────────────┘       │
└─────────────────────────────────────────────────────────────┘
            │                           │
            ▼                           ▼
┌──────────────────────┐    ┌──────────────────────┐
│   File System        │    │    SQLite DB         │
│   plugins/           │    │    data/registry.db  │
│   *.zip files        │    │    downloads table   │
└──────────────────────┘    └──────────────────────┘
```

### 2.2 目录结构

```
D:\registry\
├── main.py                 # 主应用程序入口
├── database.py            # 数据库操作模块
├── requirements.txt       # Python依赖包
├── docker-compose.yml     # Docker Compose配置
├── Dockerfile            # Docker构建文件
├── .env                  # 环境变量配置
├── plugins/              # 插件存储目录（ZIP文件）
├── data/                 # 数据库目录
│   └── registry.db      # SQLite数据库文件
├── templates/            # HTML模板
│   ├── index.html       # 主页面
│   ├── admin_upload.html # 管理上传页面
│   ├── login.html       # 登录页面
│   ├── stats.html       # 统计页面
│   └── install_guide.html # 安装指南
├── static/              # 静态文件目录（CSS/JS）
└── docs/                # 文档目录
    └── plans/           # 设计文档
```

---

## 3. 数据模型设计

### 3.1 文件存储结构

**当前结构**:
```
plugins/
├── skill-name-1.0.0.zip
├── skill-name-1.1.0.zip
└── another-skill-2.0.0.zip
```

**文件命名规范**: `{skill-name}-{version}.zip`

**示例**:
- `ask-questions-if-underspecified-1.0.0.zip`
- `semgrep-rule-creator-1.1.0.zip`

### 3.2 数据库表结构

#### downloads 表

记录所有插件下载行为

```sql
CREATE TABLE IF NOT EXISTS downloads (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    skill_name TEXT NOT NULL,           -- 技能名称
    version TEXT NOT NULL,              -- 版本号
    filename TEXT NOT NULL,             -- 文件名
    downloaded_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,  -- 下载时间
    ip_address TEXT,                    -- 下载者IP
    user_agent TEXT                     -- 用户代理
);

-- 索引：优化按技能名称和日期查询
CREATE INDEX IF NOT EXISTS idx_downloads_skill_date
ON downloads(skill_name, downloaded_at);
```

### 3.3 插件元数据结构

从 `package.json` 提取的元数据:

```json
{
    "name": "skill-name",
    "version": "1.0.0",
    "description": "Skill description",
    "author": {
        "name": "Author Name"
    },
    "title": "Display Title",
    "summary": "Short summary"
}
```

---

## 4. API 接口文档

### 4.1 公开 API

| 端点 | 方法 | 功能 | 说明 |
|------|------|------|------|
| `/` | GET | 主页面 | Web界面展示所有技能 |
| `/install-guide` | GET | 安装指南 | 安装配置说明页面 |
| `/marketplace.json` | GET | 市场索引 | Claude Code兼容的市场清单 |
| `/plugins/{filename}` | GET | 下载插件 | 下载指定ZIP文件，记录下载统计 |
| `/api/skills` | GET | 技能列表 | 分页获取技能列表 |
| `/api/stats/top` | GET | 下载统计 | 获取下载排行榜 |
| `/api/stats/export` | GET | 导出统计 | 导出Excel统计表 |
| `/api/batch-download` | POST | 批量下载 | 打包多个插件下载 |
| `/api/health` | GET | 健康检查 | 服务健康状态检查 |
| `/stats` | GET | 统计页面 | 下载统计可视化页面 |

### 4.2 管理员 API

| 端点 | 方法 | 功能 | 权限要求 |
|------|------|------|----------|
| `/admin/login` | GET/POST | 登录/登录处理 | 公开访问 |
| `/admin/logout` | GET | 登出 | 需要认证 |
| `/admin/upload` | GET/POST | 上传页面/上传插件 | 需要认证 |
| `/admin/upload-batch` | POST | 批量上传插件 | 需要认证 |
| `/admin/plugins/{filename}` | DELETE | 删除插件 | 需要认证 |

### 4.3 API 详细说明

#### 4.3.1 GET /marketplace.json

获取 Claude Code 市场索引文件

**响应示例**:
```json
{
    "name": "private-registry",
    "owner": {
        "name": "Internal Registry",
        "email": "admin@company.local"
    },
    "metadata": {
        "version": "1.0.0",
        "description": "Internal Claude Code Skill Registry",
        "updated_at": "2025-02-03T10:30:00"
    },
    "plugins": [
        {
            "name": "ask-questions-if-underspecified",
            "version": "1.0.1",
            "description": "Ask questions when task description is underspecified",
            "author": {"name": "Unknown"},
            "download_url": "http://localhost:28000/plugins/ask-questions-if-underspecified-1.0.1.zip",
            "size_kb": 5.2
        }
    ]
}
```

#### 4.3.2 GET /api/skills

获取技能列表（支持分页）

**查询参数**:
- `page`: 页码（默认: 1）
- `per_page`: 每页数量（默认: 1000）

**响应示例**:
```json
{
    "data": [
        {
            "name": "skill-name",
            "metadata": {
                "name": "skill-name",
                "version": "1.0.0",
                "description": "Description",
                "author": {"name": "Author"}
            },
            "versions": [
                {
                    "version": "1.0.0",
                    "filename": "skill-name-1.0.0.zip",
                    "size": 5324,
                    "updated_at": "2025-02-03T10:00:00"
                }
            ],
            "latest_version": "1.0.0"
        }
    ],
    "total": 50,
    "page": 1,
    "per_page": 1000,
    "total_pages": 1
}
```

#### 4.3.3 GET /plugins/{filename}

下载插件ZIP文件

**参数**:
- `filename`: ZIP文件名（如: `skill-name-1.0.0.zip`）

**响应**:
- 成功: 返回 ZIP 文件流
- 失败: 404 Not Found

**副作用**: 自动记录下载行为到数据库

#### 4.3.4 POST /admin/upload

上传单个插件

**请求**: multipart/form-data
- `file`: ZIP 文件

**响应**: HTML 页面重定向

**验证规则**:
1. 文件必须是 ZIP 格式
2. ZIP 中必须包含 package.json
3. package.json 必须包含 name 和 version 字段

#### 4.3.5 POST /admin/upload-batch

批量上传插件

**请求**: multipart/form-data
- `files`: 多个 ZIP 文件

**响应**: 批量上传结果统计

#### 4.3.6 DELETE /admin/plugins/{filename}

删除插件

**参数**:
- `filename`: 要删除的文件名

**响应示例**:
```json
{
    "success": true,
    "message": "Deleted skill-name-1.0.0.zip"
}
```

#### 4.3.7 GET /api/stats/top

获取下载统计排行榜

**查询参数**:
- `start_date`: 开始日期（YYYY-MM-DD，可选）
- `end_date`: 结束日期（YYYY-MM-DD，可选）

**响应示例**:
```json
{
    "period": {
        "start_date": "all-time",
        "end_date": "all-time"
    },
    "total_downloads": 1250,
    "rankings": [
        {
            "skill_name": "skill-name",
            "author": "Author Name",
            "downloads": 350
        }
    ]
}
```

#### 4.3.8 GET /api/stats/export

导出统计数据为 Excel

**查询参数**:
- `start_date`: 开始日期（YYYY-MM-DD，可选）
- `end_date`: 结束日期（YYYY-MM-DD，可选）

**响应**: Excel 文件流

**文件名格式**: `download_stats_{start}_to_{end}.xlsx`

#### 4.3.9 POST /api/batch-download

批量下载插件打包

**请求体**:
```json
{
    "filenames": ["skill1-1.0.0.zip", "skill2-1.0.0.zip"]
}
```

**响应**: ZIP 文件流

**文件名格式**: `skills-batch-{timestamp}.zip`

#### 4.3.10 GET /api/health

健康检查端点

**响应示例**:
```json
{
    "status": "healthy",
    "timestamp": "2025-02-03T10:30:00",
    "version": "1.0.0",
    "service": "Skill Registry"
}
```

**错误状态**:
```json
{
    "status": "unhealthy",
    "error": "Plugins directory not accessible"
}
```

---

## 5. 核心模块设计

### 5.1 插件扫描模块 (main.py:105-143)

**功能**: 扫描插件目录并提取元数据

```python
def scan_plugins() -> List[dict]:
    """Scan plugins directory and return metadata list."""
    # 1. 遍历 plugins/*.zip
    # 2. 解析文件名提取 skill_name 和 version
    # 3. 提取元数据
    # 4. 返回排序后的插件列表
```

**算法流程**:
1. 扫描 `plugins/` 目录下所有 `.zip` 文件
2. 解析文件名获取技能名和版本号
3. 按技能名分组，收集所有版本
4. 从最新版本提取元数据
5. 返回排序后的列表

### 5.2 文件名解析模块 (main.py:76-102)

**功能**: 解析插件文件名

```python
def parse_plugin_filename(filename: str) -> tuple[str, str]:
    """Parse plugin filename to extract name and version.
    Format: {skill-name}-{version}.zip
    Returns: (skill_name, version)
    """
```

**解析逻辑**:
1. 移除 `.zip` 扩展名
2. 从右向左查找版本模式（包含数字和点）
3. 分离技能名和版本号

**示例**:
- `ask-questions-if-underspecified-1.0.0.zip` → `("ask-questions-if-underspecified", "1.0.0")`
- `semgrep-rule-creator-1.1.0.zip` → `("semgrep-rule-creator", "1.1.0")`

### 5.3 元数据提取模块 (main.py:146-196)

**功能**: 从 ZIP 文件中提取元数据

```python
def extract_metadata(zip_filename: str) -> Optional[dict]:
    """Extract metadata from package.json inside zip.
    Supports nested directory structure.
    """
```

**提取策略**:
1. 首先尝试在根目录查找 `package.json`
2. 如果未找到，尝试在子目录中查找（如 `skill-name/package.json`）
3. 如果都失败，返回基础信息作为回退

### 5.4 插件验证模块 (main.py:343-398)

**功能**: 验证上传的 ZIP 文件

```python
def validate_skill_zip(zip_path: Path) -> tuple[bool, dict]:
    """Validate a skill ZIP file.
    Returns: (is_valid, metadata_or_error_info)
    """
```

**验证规则**:
1. 文件必须是有效的 ZIP 格式
2. ZIP 中必须包含 `package.json`
3. `package.json` 必须包含 `name` 字段
4. `package.json` 必须包含 `version` 字段

### 5.5 下载统计模块 (database.py)

**功能**: 记录和查询下载统计

**主要函数**:

```python
def record_download(skill_name, version, filename, ip_address, user_agent) -> int:
    """记录下载事件，返回记录ID"""

def get_download_stats(start_date, end_date) -> Dict:
    """获取指定时间范围的下载统计"""

def get_stats_with_author(plugins, start_date, end_date) -> Dict:
    """获取带作者信息的下载统计"""
```

---

## 6. 前端设计

### 6.1 主界面 (templates/index.html)

**功能特性**:
- 响应式设计，支持移动端
- 实时搜索和排序
- 批量选择和下载
- 分页显示
- 下载统计展示

**界面组件**:
1. **固定头部工具栏**
   - 搜索框
   - 排序选择器
   - 批量下载按钮
   - 管理入口链接

2. **技能列表表格**
   - 复选框（批量选择）
   - 技能名称和描述
   - 版本信息
   - 作者信息
   - 下载次数
   - 操作按钮（下载、安装命令）

3. **分页导航**
   - 页码跳转
   - 每页显示数量选择

### 6.2 管理界面 (templates/admin_upload.html)

**功能特性**:
- 单个/批量上传模式切换
- 文件验证和错误提示
- 上传进度显示
- 成功/失败消息反馈

**界面组件**:
1. **模式切换器**
   - 单个上传模式
   - 批量上传模式

2. **上传表单**
   - 文件选择器（单个/多个）
   - 提交按钮

3. **消息反馈区**
   - 成功消息（绿色）
   - 错误消息（红色）

### 6.3 统计界面 (templates/stats.html)

**功能特性**:
- 日期范围选择
- 下载排行榜展示
- Excel 导出功能

---

## 7. 认证与安全

### 7.1 认证机制

**Session-based 认证**:
- 基于 Cookie 的会话管理
- 登录后 Session 中存储用户名
- 需要认证的接口通过 `require_auth` 依赖检查

**认证流程**:
```python
# 登录
POST /admin/login
→ 验证用户名密码
→ 设置 Session
→ 重定向到 /admin/upload

# 访问受保护资源
GET /admin/upload
→ require_auth 检查 Session
→ 有效: 返回页面
→ 无效: 重定向到 /admin/login
```

### 7.2 安全配置

**环境变量**:

| 变量名 | 默认值 | 说明 |
|--------|--------|------|
| `ADMIN_USERNAME` | `admin` | 管理员用户名 |
| `ADMIN_PASSWORD` | `admin123` | 管理员密码 |
| `SECRET_KEY` | `your-secret-key-change-this` | Session 密钥 |
| `PLUGINS_DIR` | `./plugins` | 插件存储目录 |

**安全建议**:
1. 生产环境必须修改默认密码
2. 使用强随机字符串作为 SECRET_KEY
3. 使用反向代理（Nginx）处理 HTTPS
4. 定期备份插件目录和数据库

---

## 8. 部署配置

### 8.1 Docker 部署

**Dockerfile**:
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
EXPOSE 8000
CMD ["python", "main.py"]
```

**docker-compose.yml**:
```yaml
version: '3.8'
services:
  registry:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./plugins:/app/plugins
      - ./data:/app/data
    environment:
      - ADMIN_USERNAME=${ADMIN_USERNAME}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
      - SECRET_KEY=${SECRET_KEY}
    restart: unless-stopped
```

**启动命令**:
```bash
docker-compose up -d
```

### 8.2 环境变量配置

创建 `.env` 文件:
```env
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password
SECRET_KEY=your-random-secret-key-here
PLUGINS_DIR=./plugins
```

### 8.3 依赖包 (requirements.txt)

```
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
jinja2>=3.1.2
python-dotenv>=1.0.0
openpyxl>=3.1.2
```

---

## 9. 安装与使用

### 9.1 安装步骤

1. **克隆仓库**
```bash
git clone <repository-url>
cd registry
```

2. **安装依赖**
```bash
pip install -r requirements.txt
```

3. **配置环境变量**
```bash
cp .env.example .env
# 编辑 .env 文件，设置用户名和密码
```

4. **启动服务**
```bash
python main.py
```

5. **访问服务**
- Web界面: http://localhost:28000
- Marketplace: http://localhost:28000/marketplace.json

### 9.2 配置 Claude Code

在 Claude Code 中添加私有市场:

```bash
/plugins marketplace add http://your-server:28000/marketplace.json
```

或通过环境变量:
```env
CLAUDE_MARKETPLACE=http://your-server:28000/marketplace.json
```

---

## 10. 未来规划

### 10.1 计划中的功能

根据 `docs/plans/2025-02-01-add-organization-dimension.md`:

1. **组织维度支持**
   - 目录结构: `plugins/{org}/{skill}/{version}.zip`
   - API 支持组织参数
   - 向后兼容（现有插件归入 `default` 组织）

2. **用户认证增强**
   - 支持多用户
   - 基于角色的访问控制
   - OAuth2 集成

3. **高级统计**
   - 用户下载趋势图
   - 技能使用情况分析
   - 导出多种格式

### 10.2 技术改进

1. **性能优化**
   - 缓存插件扫描结果
   - 数据库查询优化
   - 静态资源 CDN

2. **可扩展性**
   - 支持云存储（S3/OSS）
   - PostgreSQL 数据库支持
   - 分布式部署

3. **监控与运维**
   - 日志聚合
   - 性能监控
   - 告警系统

---

## 11. 常见问题

### 11.1 上传失败

**问题**: 上传 ZIP 文件后显示 "Validation failed"

**解决方案**:
1. 确保 ZIP 中包含 `package.json`
2. 检查 `package.json` 是否有 `name` 和 `version` 字段
3. 确认 ZIP 文件格式正确

### 11.2 Claude Code 无法连接

**问题**: Claude Code 无法识别市场

**解决方案**:
1. 检查 `/marketplace.json` 是否可访问
2. 验证 JSON 格式是否正确
3. 确认网络连接和防火墙设置

### 11.3 下载统计不显示

**问题**: 统计页面显示无数据

**解决方案**:
1. 检查数据库文件 `data/registry.db` 是否存在
2. 确认有下载行为发生
3. 检查数据库权限

---

## 12. 技术支持

- **项目地址**: [GitHub Repository]
- **文档**: `docs/` 目录
- **问题反馈**: 通过 Issue 提交

---

*文档版本: 1.0.0*
*更新日期: 2025-02-03*
